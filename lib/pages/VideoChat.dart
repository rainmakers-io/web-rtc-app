import 'package:flutter/material.dart';

class PageVideoChat extends StatelessWidget {
  PageVideoChat({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('비디오 채팅')),
    );
  }
}
